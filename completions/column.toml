# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "column"
description = "Columnate lists."

[[flags]]
short = "-c"
long = "--columns"
description = "output is formatted for a display width"
takes_value = true

[[flags]]
short = "-t"
long = "--table"
description = "determine the number of columns the input contains"

[[flags]]
short = "-s"
long = "--separator"
description = "specify the possible input item delimiters"
takes_value = true

[[flags]]
long = "--output-separator"
description = "specify the columns separator for table output"
takes_value = true

[[flags]]
short = "-o"
description = "specify the columns separator for table output"

[[flags]]
long = "--table-name"
description = "specify the table name for JSON or XML output"
takes_value = true

[[flags]]
short = "-n"
description = "specify the table name for JSON or XML output"

[[flags]]
long = "--output-fields"
description = "specify which columns to include in JSON or XML output"
takes_value = true

[[flags]]
short = "-x"
description = "specify which columns to include in JSON or XML output"

[[flags]]
long = "--table-right"
description = "columns to right align in table output"
takes_value = true

[[flags]]
short = "-r"
description = "right align text in table columns"

[[flags]]
short = "-R"
description = "columns to right align in table output"

[[flags]]
long = "--table-hide"
description = "don't print header in table output"

[[flags]]
short = "-H"
description = "don't print header in table output"

[[flags]]
long = "--table-empty"
description = "don't use empty lines in table output"

[[flags]]
short = "-e"
description = "don't use empty lines in table output"

[[flags]]
long = "--table-no-trunc"
description = "don't truncate text in table output"

[[flags]]
short = "-N"
description = "don't truncate text in table output"

[[flags]]
long = "--table-noescape"
description = "don't escape newline, tab, backslash in table output"

[[flags]]
short = "-E"
description = "don't escape newline, tab, backslash in table output"

[[flags]]
long = "--json"
description = "use JSON output format for table"

[[flags]]
short = "-J"
description = "use JSON output format for table"

[[flags]]
long = "--output-width"
description = "maximum display width"
takes_value = true

[[flags]]
short = "-O"
description = "maximum display width"

[[flags]]
long = "--version"
description = "output version information and exit"

[[flags]]
short = "-V"
description = "output version information and exit"

[[flags]]
long = "--help"
description = "display this help and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
