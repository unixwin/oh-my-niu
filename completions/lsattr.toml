# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "lsattr"
description = "List Windows file attributes in a stable flag string. Flags are RHSADCEITOPL for readonly, hidden, system, archive, directory, compressed, encrypted, not indexed, temporary, offline, sparse, and reparse point."

[[flags]]
short = "-R"
long = "--recursive"
description = "recursively list attributes of directories"

[[flags]]
short = "-d"
long = "--directory"
description = "list directories themselves, not contents"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
