# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "file"
description = "The file command tests each argument in an attempt to classify it. There are three sets of tests, performed in this order: filesystem tests, magic number tests, and language tests."

[[flags]]
short = "-b"
long = "--brief"
description = "do not prepend filenames to output lines"

[[flags]]
short = "-h"
long = "--no-dereference"
description = "don't follow symlinks"

[[flags]]
short = "-i"
long = "--mime"
description = "output MIME type strings [TODO]"

[[flags]]
short = "-L"
long = "--dereference"
description = "follow symlinks"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
