# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "numfmt"
description = "Convert numbers from/to human-readable strings. Convert numbers to/from human-readable strings (e.g., 1K, 1M). If no number is specified, read from standard input."

[[flags]]
short = "-d"
long = "--delimiter"
description = "use X instead of whitespace for field delimiter"
takes_value = true

[[flags]]
long = "--from"
description = "autoconvert from X"
takes_value = true

[[flags]]
long = "--to"
description = "autoconvert to X"
takes_value = true

[[flags]]
long = "--round"
description = "use METHOD for rounding"
takes_value = true

[[flags]]
long = "--padding"
description = "pad numbers to width N"
takes_value = true

[[flags]]
long = "--pad"
description = "pad numbers to width N"
takes_value = true

[[flags]]
long = "--suffix"
description = "add STRING after formatted numbers"
takes_value = true

[[flags]]
long = "--field"
description = "replace numbers in field N"
takes_value = true

[[flags]]
short = "-f"
long = "--format"
description = "use printf style floating-point FORMAT"
takes_value = true

[[flags]]
long = "--header"
description = "print the first N header lines unchanged"
takes_value = true

[[flags]]
long = "--grouping"
description = "group digits with locale thousands separator"

[[flags]]
long = "--invalid"
description = "set policy for invalid values: 'abort' (default), 'warn', 'ignore'"
takes_value = true
values = ["abort", "warn", "ignore"]

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
