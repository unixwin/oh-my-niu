# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "diff3"
description = "Compare three files. Compare three files line by line and report differences. MINE is your file, OLDER is the common ancestor, YOURS is the other file. By default, outputs conflicts between MINE and YOURS."

[[flags]]
short = "-e"
description = "output ed script"

[[flags]]
short = "-E"
description = "output ed script with bracketed conflicts"

[[flags]]
short = "-m"
description = "output in merged format"

[[flags]]
short = "-A"
description = "like -E, but overwrite overlapping changes"

[[flags]]
short = "-a"
description = "treat all files as text"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
