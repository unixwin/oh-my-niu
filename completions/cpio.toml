# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "cpio"
description = "Copy files to and from archives. Copy files to and from cpio archives. Supports creating, extracting, and listing archive contents. Uses new ASCII format (070701/070702)."

[[flags]]
short = "-o"
long = "--create"
description = "create archive"

[[flags]]
short = "-i"
long = "--extract"
description = "extract archive"

[[flags]]
short = "-t"
long = "--list"
description = "list archive contents"

[[flags]]
short = "-d"
description = "create leading directories where needed"

[[flags]]
short = "-m"
description = "preserve modification time"

[[flags]]
short = "-v"
long = "--verbose"
description = "verbose output"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
