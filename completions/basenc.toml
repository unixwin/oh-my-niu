# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "basenc"
description = "Encode or decode FILE, or standard input, using the selected encoding. With no FILE, or when FILE is -, read standard input. ENCODING is one of --base64, --base64url, --base32, --base32hex, --base16, --base2lsbf, --base2msbf, --base58, or --z85."

[[flags]]
short = "-d"
long = "--decode"
description = "decode data"

[[flags]]
short = "-i"
long = "--ignore-garbage"
description = "when decoding, ignore non-alphabet characters"

[[flags]]
short = "-w"
long = "--wrap"
description = "wrap encoded lines after COLS character (default 76). Use 0 to disable line wrapping"
takes_value = true

[[flags]]
short = "-b"
description = "legacy baseN selector"
takes_value = true

[[flags]]
long = "--base64"
description = "same as base64 program"

[[flags]]
long = "--base64url"
description = "file- and URL-safe base64"

[[flags]]
long = "--base58"
description = "base58 encoding"

[[flags]]
long = "--base32"
description = "same as base32 program"

[[flags]]
long = "--base32hex"
description = "extended hex alphabet base32"

[[flags]]
long = "--base16"
description = "hexadecimal encoding"

[[flags]]
long = "--base2lsbf"
description = "bit string, least significant bit first"

[[flags]]
long = "--base2msbf"
description = "bit string, most significant bit first"

[[flags]]
long = "--z85"
description = "Z85 encoding"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
