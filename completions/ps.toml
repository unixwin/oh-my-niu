# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "ps"
description = "Report a snapshot of the current processes. Shows information about running processes on Windows."

[[flags]]
short = "-e"
description = "select all processes (same as -A)"

[[flags]]
short = "-A"
description = "select all processes"

[[flags]]
short = "-a"
description = "select all processes except session leaders and not associated with a terminal"

[[flags]]
short = "-f"
description = "do full-format listing"

[[flags]]
short = "-l"
description = "long format"

[[flags]]
short = "-u"
description = "display user-oriented format"

[[flags]]
long = "--user"
description = "filter processes by user name"
takes_value = true

[[flags]]
short = "-x"
description = "lift the BSD-style \"must have a tty\" restriction"

[[flags]]
short = "-w"
description = "wide output (do not truncate command lines)"

[[flags]]
long = "--no-headers"
description = "print no header line"

[[flags]]
long = "--sort"
description = "sort by column (e.g., +pid, -rss)"
takes_value = true

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
