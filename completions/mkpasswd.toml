# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "mkpasswd"
description = "Generate passwd-like entries from Windows accounts."

[[flags]]
short = "-c"
long = "--current"
description = "print only the current process user"

[[flags]]
short = "-l"
long = "--local"
description = "enumerate local machine users (default)"

[[flags]]
short = "-p"
long = "--path-prefix"
description = "home directory prefix"
takes_value = true
values_from = "path"

[[flags]]
short = "-s"
long = "--shell"
description = "login shell path"
takes_value = true
values_from = "path"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
