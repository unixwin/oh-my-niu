# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "logger"
description = "Write messages to the Windows event/logging facilities."

[[flags]]
short = "-p"
long = "--priority"
description = "message priority"
takes_value = true

[[flags]]
short = "-t"
long = "--tag"
description = "mark every line with this tag"
takes_value = true

[[flags]]
short = "-s"
long = "--stderr"
description = "also write the message to standard error"

[[flags]]
long = "--id"
description = "accepted for compatibility"
takes_value = true

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
