# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "pkill"
description = "Signal processes selected by name or command line. On Windows, TERM/KILL are mapped to Win32 termination."

[[flags]]
short = "-f"
long = "--full"
description = "match against full command line"

[[flags]]
short = "-i"
long = "--ignore-case"
description = "match case insensitively"

[[flags]]
short = "-x"
long = "--exact"
description = "match the whole name or command line"

[[flags]]
short = "-e"
long = "--echo"
description = "display what is killed"

[[flags]]
short = "-0"
description = "only check for matching processes"

[[flags]]
short = "-9"
description = "send SIGKILL; mapped to Win32 termination"

[[flags]]
short = "-TERM"
description = "send SIGTERM; mapped to Win32 termination"

[[flags]]
short = "-SIGTERM"
description = "send SIGTERM; mapped to Win32 termination"

[[flags]]
short = "-KILL"
description = "send SIGKILL; mapped to Win32 termination"

[[flags]]
short = "-SIGKILL"
description = "send SIGKILL; mapped to Win32 termination"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
