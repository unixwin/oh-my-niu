# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "top"
description = "The top program provides a dynamic real-time view of a running system. It can display system summary information as well as a list of processes or threads currently being managed by the Linux kernel."

[[flags]]
short = "-b"
long = "--batch"
description = "batch mode: don't accept input, run until -n iterations"

[[flags]]
short = "-d"
long = "--delay"
description = "delay between updates, in seconds"
takes_value = true

[[flags]]
short = "-n"
long = "--iterations"
description = "number of iterations before exiting"
takes_value = true

[[flags]]
short = "-p"
long = "--pid"
description = "monitor only processes with given PIDs"
takes_value = true

[[flags]]
short = "-u"
long = "--user"
description = "monitor only processes with given user"
takes_value = true

[[flags]]
short = "-U"
long = "--User"
description = "monitor only processes with real user ID/name"
takes_value = true

[[flags]]
short = "-s"
long = "--secure-mode"
description = "secure mode: disables some features"

[[flags]]
short = "-c"
long = "--command"
description = "show command line instead of process name"

[[flags]]
short = "-H"
long = "--threads"
description = "show threads as if they were processes"

[[flags]]
short = "-o"
long = "--field-sort"
description = "override sort field"
takes_value = true

[[flags]]
short = "-w"
long = "--width"
description = "override output width"
takes_value = true

[[flags]]
long = "--rows"
description = "limit number of processes displayed"
takes_value = true

[[flags]]
short = "-v"
long = "--version"
description = "print version information"

[[flags]]
long = "--help"
description = "display this help 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
