# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "echo"
description = "Write arguments to the standard output."

[[flags]]
short = "-n"
description = "do not append a newline"

[[flags]]
short = "-e"
description = "enable interpretation of backslash escapes"

[[flags]]
short = "-E"
description = "explicitly suppress interpretation of backslash escapes \\a        alert (bell) \\b        backspace \\c        suppress further output \\e        escape character \\E        escape character \\f        form feed \\n        new line \\r        carriage return \\t        horizontal tab \\v        vertical tab \\\\        backslash \\0nnn     the character whose ASCII code is NNN (octal).  NNN can be 0 to 3 octal digits \\xHH      the eight-bit character whose value is HH (hexadecimal).  HH can be one or two hex digits \\uHHHH    the Unicode character whose value is the hexadecimal value HHHH. HHHH can be one to four hex digits. (Not fully implemented on Windows) \\UHHHHHHHH the Unicode character whose value is the hexadecimal value HHHHHHHH. HHHHHHHH can be one to eight hex digits. (Not fully implemented on Windows)"

[[flags]]
short = "-u"
long = "--upper"
description = "convert text to uppercase"

[[flags]]
short = "-r"
long = "--repeat"
description = "repeat output N times"
takes_value = true

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
