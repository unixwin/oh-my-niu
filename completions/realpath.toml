# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "realpath"
description = "Print the resolved absolute path for each FILE. If no FILE is given, print the resolved absolute path of the current directory."

[[flags]]
short = "-E"
long = "--canonicalize"
description = "all but the last component must exist"

[[flags]]
short = "-m"
long = "--canonicalize-missing"
description = "no path components need to exist"

[[flags]]
short = "-e"
long = "--canonicalize-existing"
description = "all components must exist"

[[flags]]
short = "-L"
long = "--logical"
description = "resolve '..' and '.' before symlinks"

[[flags]]
short = "-P"
long = "--physical"
description = "resolve symlinks before '..' and '.'"

[[flags]]
short = "-q"
long = "--quiet"
description = "suppress most error messages"

[[flags]]
short = "-s"
long = "--strip"
description = "do not expand symlinks"

[[flags]]
long = "--relative-to"
description = "print the resolved path relative to DIR"
takes_value = true
values_from = "path"

[[flags]]
long = "--relative-base"
description = "print relative paths below DIR"
takes_value = true
values_from = "path"

[[flags]]
short = "-z"
long = "--zero"
description = "end output with NUL instead of newline"

[[flags]]
long = "--no-symlinks"
description = "do not expand symlinks"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
