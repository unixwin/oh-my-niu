# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "getopt"
description = "Parse command options and print a shell-compatible argument list. This native subset supports short options, required ':' arguments, and optional '::' arguments."

[[flags]]
short = "-o"
long = "--options"
description = "short option specification"
takes_value = true

[[flags]]
short = "-n"
long = "--name"
description = "name used in diagnostics"
takes_value = true

[[flags]]
short = "-q"
long = "--quiet"
description = "disable error reporting by getopt"

[[flags]]
short = "-u"
long = "--unquoted"
description = "do not quote the output"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
