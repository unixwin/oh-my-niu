# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "shred"
description = "Overwrite the specified FILE(s) repeatedly to help prevent data recovery."

[[flags]]
short = "-f"
long = "--force"
description = "ignore write-protection"

[[flags]]
short = "-n"
long = "--iterations"
description = "overwrite N times (default 3)"
takes_value = true

[[flags]]
short = "-u"
long = "--remove"
description = "truncate and remove file after overwriting"

[[flags]]
short = "-z"
long = "--zero"
description = "add a final overwrite with zeros to hide shredding"

[[flags]]
short = "-v"
long = "--verbose"
description = "show progress"

[[flags]]
long = "--random-source"
description = "use FILE as the source of random data"
takes_value = true
values_from = "path"

[[flags]]
short = "-s"
long = "--size"
description = "shred only BYTES bytes instead of the whole file"
takes_value = true
values_from = "path"

[[flags]]
short = "-x"
long = "--exact"
description = "do not round file size up to the next full block"

[[flags]]
long = "--remove"
description = "truncate and remove FILE after overwriting; HOW can be 'unlink' or 'wipe'"
takes_value = true
values = ["unlink", "wipe"]

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
