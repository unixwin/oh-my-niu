# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "od"
description = "Dump files in octal and other formats. Write an unambiguous representation of each FILE to standard output. With no FILE, or when FILE is -, read standard input."

[[flags]]
short = "-A"
description = "select input base"
takes_value = true

[[flags]]
short = "-a"
description = "select named character output"

[[flags]]
short = "-b"
description = "select octal byte output"

[[flags]]
short = "-c"
description = "select ASCII output"

[[flags]]
short = "-d"
description = "select unsigned decimal 2-byte output"

[[flags]]
short = "-j"
description = "skip bytes"
takes_value = true

[[flags]]
short = "-N"
description = "limit bytes"
takes_value = true

[[flags]]
short = "-o"
description = "select octal 2-byte output"

[[flags]]
short = "-t"
description = "select output type"
takes_value = true

[[flags]]
short = "-v"
description = "write all input data"

[[flags]]
short = "-w"
long = "--width"
description = "output bytes per line"
takes_value = true

[[flags]]
short = "-x"
description = "select hexadecimal 2-byte units"

[[flags]]
long = "--traditional"
description = "accept arguments in traditional form (e.g., od -x file)"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
