# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "runcon"
description = "or:  runcon [-c] [-u USER] [-r ROLE] [-t TYPE] [-l RANGE] [COMMAND [ARG]...]"

[[flags]]
short = "-c"
long = "--compute"
description = "compute process transition context before modifying"

[[flags]]
short = "-u"
long = "--user"
description = "set user component of the context"
takes_value = true

[[flags]]
short = "-r"
long = "--role"
description = "set role component of the context"
takes_value = true

[[flags]]
short = "-t"
long = "--type"
description = "set type component of the context"
takes_value = true

[[flags]]
short = "-l"
long = "--range"
description = "set range component of the context"
takes_value = true

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
