# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "lsof"
description = "List open files for running processes on Windows. This command enumerates kernel handles and prints entries that map to files. Some handles may require elevated privileges and some object queries can timeout."

[[flags]]
short = "-p"
long = "--pid"
description = "list files opened by process id"
takes_value = true
values_from = "path"

[[flags]]
short = "-a"
long = "--all"
description = "include non-file handles and unnamed entries"

[[flags]]
short = "-i"
long = "--internet"
description = "show internet connections; optional spec ':PORT', 'PROTO', 'PROTO:PORT'"
takes_value = true

[[flags]]
short = "-F"
long = "--field"
description = "machine-readable field output"

[[flags]]
short = "-n"
long = "--numeric"
description = "do not convert native paths to DOS paths"

[[flags]]
long = "--no-headers"
description = "print no header line"

[[flags]]
short = "-t"
long = "--timeout-ms"
description = "NtQueryObject timeout in milliseconds"
takes_value = true

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
