# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "tail"
description = "Print the last 10 lines of each FILE to standard output. With more than one FILE, precede each with a header giving the file name."

[[flags]]
short = "-c"
long = "--bytes"
description = "output the last NUM bytes; or use -c +NUM to output starting with byte NUM of each file"
takes_value = true
values_from = "path"

[[flags]]
long = "--debug"
description = "output extra follow diagnostics to stderr"

[[flags]]
short = "-f"
description = "output appended data as the file grows"

[[flags]]
long = "--follow"
description = "output appended data as the file grows; with"
takes_value = true
values_from = "path"

[[flags]]
short = "-F"
description = "same as --follow=name --retry"

[[flags]]
short = "-n"
long = "--lines"
description = "output the last NUM lines, instead of the last 10; or use -n +NUM to skip NUM-1 lines at the start"
takes_value = true

[[flags]]
long = "--max-unchanged-stats"
description = "with --follow=name, reopen a FILE which has not changed size after N iterations to see if it has been renamed [IMPLEMENTED]"
takes_value = true
values_from = "path"

[[flags]]
long = "--pid"
description = "with -f, terminate after process ID, PID dies [IMPLEMENTED]"
takes_value = true

[[flags]]
short = "-q"
long = "--quiet"
description = "never output headers giving file names"

[[flags]]
long = "--silent"
description = "never output headers giving file names"

[[flags]]
long = "--retry"
description = "keep trying to open a file if it is inaccessible"

[[flags]]
long = "--use-polling"
description = "disable native directory change watching and use polling instead"

[[flags]]
short = "-s"
long = "--sleep-interval"
description = "with -f, sleep for approximately N seconds between iterations"
takes_value = true

[[flags]]
short = "-v"
long = "--verbose"
description = "always output headers giving file names"

[[flags]]
short = "-z"
long = "--zero-terminated"
description = "line delimiter is NUL, not newline"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
