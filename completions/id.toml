# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "id"
description = "Print user and group information for the specified USER, or (when USER omitted) for the current user."

[[flags]]
short = "-a"
description = "ignore, for compatibility with other versions"

[[flags]]
short = "-g"
long = "--group"
description = "print only the effective group ID"

[[flags]]
short = "-G"
long = "--groups"
description = "print all group IDs"

[[flags]]
short = "-n"
long = "--name"
description = "print a name instead of a number"

[[flags]]
short = "-r"
long = "--real"
description = "print the real ID instead of the effective ID"

[[flags]]
short = "-u"
long = "--user"
description = "print only the effective user ID"

[[flags]]
short = "-Z"
long = "--context"
description = "print only the security context (not implemented)"

[[flags]]
long = "--zero"
description = "delimit entries with NUL, not whitespace"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
