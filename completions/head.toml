# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "head"
description = "Print the first 10 lines of each FILE to standard output. With more than one FILE, precede each with a header giving the file name."

[[flags]]
short = "-c"
long = "--bytes"
description = "print the first NUM bytes of each file; with the leading '-', print all but the last NUM bytes"
takes_value = true
values_from = "path"

[[flags]]
short = "-n"
long = "--lines"
description = "print the first NUM lines instead of the first 10; with the leading '-', print all but the last NUM lines"
takes_value = true

[[flags]]
short = "-q"
long = "--quiet"
description = "never print headers giving file names for multiple files"

[[flags]]
long = "--silent"
description = "never print headers giving file names for multiple files"

[[flags]]
short = "-v"
long = "--verbose"
description = "always print headers giving file names for multiple files"

[[flags]]
short = "-z"
long = "--zero-terminated"
description = "line delimiter is NUL, not newline"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
