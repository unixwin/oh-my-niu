# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "env"
description = "Set each NAME to VALUE in the environment and print the resulting environment, or run COMMAND in that environment."

[[flags]]
short = "-i"
long = "--ignore-environment"
description = "start with an empty environment"

[[flags]]
short = "-u"
long = "--unset"
description = "remove variable from the environment"
takes_value = true

[[flags]]
short = "-0"
long = "--null"
description = "end each output line with NUL, not newline"

[[flags]]
short = "-f"
long = "--file"
description = "read and set variables from a .env-style configuration file"
takes_value = true
values_from = "path"

[[flags]]
short = "-S"
long = "--split-string"
description = "process and split S into separate arguments; useful for shebang"
takes_value = true

[[flags]]
short = "-a"
long = "--argv0"
description = "pass STRING as argv[0] to the command"
takes_value = true

[[flags]]
short = "-C"
long = "--chdir"
description = "change working directory"
takes_value = true
values_from = "path"

[[flags]]
long = "--default-signal"
description = "reset handling of SIG to its default"
takes_value = true

[[flags]]
long = "--ignore-signal"
description = "set handling of SIG to do nothing"
takes_value = true

[[flags]]
long = "--block-signal"
description = "block delivery of SIG to COMMAND"
takes_value = true

[[flags]]
long = "--list-signal-handling"
description = "list non default signal handling"

[[flags]]
short = "-v"
long = "--debug"
description = "print extra information about the processing"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
