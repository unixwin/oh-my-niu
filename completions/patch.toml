# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "patch"
description = "Apply a diff file to an original. Apply a diff file (patch) to an original file, creating a patched version. Supports unified diff format. Can also read patch from standard input."

[[flags]]
short = "-p"
description = "strip NUM leading components from file names"
takes_value = true
values_from = "path"

[[flags]]
short = "-i"
description = "read patch from FILE"
takes_value = true
values_from = "path"

[[flags]]
short = "-R"
long = "--reverse"
description = "assume patch was created with old and new files swapped"

[[flags]]
short = "-N"
long = "--forward"
description = "assume patch was created with old and new files swapped"

[[flags]]
short = "-b"
description = "back up the original file"
takes_value = true
values_from = "path"

[[flags]]
long = "--dry-run"
description = "do not actually change any files"

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
