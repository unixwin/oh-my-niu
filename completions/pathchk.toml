# Generated by scripts/generate-winuxcmd-completions.py; do not edit by hand.
command = "pathchk"
description = "Check whether file names are valid or portable. Check that each file name is valid for the current filesystem, and optionally check for POSIX portability."

[[flags]]
short = "-p"
long = "--portability"
description = "check for all POSIX systems"

[[flags]]
short = "-P"
long = "--posix"
description = "check for empty names and leading \"-\""

[[flags]]
long = "--help"
description = "display this help and exit"

[[flags]]
short = "-V"
long = "--version"
description = "output version information and exit 0  if OK, 1  if minor problems, 2  if serious trouble. WinuxCmd is a Windows implementation of GNU CoreUtils for Linux-Windows developers and AI coding assistants."
