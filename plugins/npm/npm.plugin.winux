# Oh My Niu npm plugin.

[ -f "$NIUBASH/lib/aliases.winux" ] && . "$NIUBASH/lib/aliases.winux"

niubash_load_alias_asset npm

nr() {
  npm run "$@"
}

nrd() {
  npm run dev "$@"
}

nrb() {
  npm run build "$@"
}
